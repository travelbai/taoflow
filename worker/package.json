{
  "name": "taoflow-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/index.js",
    "dev": "wrangler dev",
    "deploy": "wrangler publish"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "wrangler": "^2.21.3"
  },
  "optionalDependencies": {
    "@esbuild/win32-arm64": "^0.28.0"
  }
}
